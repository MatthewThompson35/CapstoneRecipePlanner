<style>
	body {
		background-color: #f5f5f5;
		font-family: Arial, sans-serif;
	}

	h1 {
		font-size: 32px;
		margin: 0;
	}

	.form-popup {
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		position: fixed;
		z-index: 1;
		padding-top: 100px;
		left: 0;
		top: 0;
		width: 600px;
		height: 100%;
		overflow: auto;
		background-color: rgba(0, 0, 0, 0.5);
	}

	.form-container {
		background-color: #fff;
        width: 500px;
		padding: 20px;
		border-radius: 10px;
		box-shadow: 0px 3px 6px rgba(0, 0, 0, 0.16);
	}

	.form-container label {
		font-size: 18px;
		font-weight: bold;
		display: block;
		margin-bottom: 10px;
	}

	.form-container input[type="text"],
	.form-container input[type="password"] {
		width: 100%;
		padding: 10px;
		font-size: 16px;
		margin-bottom: 20px;
		border: none;
		border-radius: 5px;
		background-color: #f5f5f5;
	}

	.form-container button {
		background-color: #33b5e5;
		color: #fff;
		font-size: 16px;
		padding: 10px 20px;
		border: none;
		border-radius: 5px;
		cursor: pointer;
		margin-right: 10px;
	}

	.form-container button.cancel {
		background-color: #d9534f;
		margin-right: 0;
	}

    body {
        background-color: #f5f5f5;
        font-family: Arial, sans-serif;
    }

    h1 {
        font-size: 32px;
        margin: 0;
    }

    .form-popup {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        position: fixed;
        z-index: 1;
        padding-top: 100px;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        overflow: auto;
        background-color: rgba(0, 0, 0, 0.5);
    }

    .form-container {
        background-color: #fff;
        max-width: 500px;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0px 3px 6px rgba(0, 0, 0, 0.16);
    }

        .form-container label {
            font-size: 18px;
            font-weight: bold;
            display: block;
            margin-bottom: 10px;
        }

        .form-container input[type="text"],
        .form-container input[type="password"] {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            margin-bottom: 20px;
            border: none;
            border-radius: 5px;
            background-color: #f5f5f5;
        }

        .form-container button {
            background-color: #33b5e5;
            color: #fff;
            font-size: 16px;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-right: 10px;
        }

            .form-container button.cancel {
                background-color: #d9534f;
                margin-right: 0;
            }

        .form-container label.error {
            font-size: 14px;
        }

</style>

<header>
    <div style="text-align: center;"> <h1> Welcome to Recipe Planner</h1> </div>
</header>

<body>
    <div class="form-popup" id="myForm">
        <form class="form-container" method="post">
            <label for="Username"><b>Username:</b></label>
            <input type="text" placeholder="Enter Username" name="username" id="username">

            <label for="Password"><b>Password:</b></label>
            <input type="password" placeholder="Enter Password" name="password" id="password">

            <label for="RepeatPassword"><b>Reenter Password:</b></label>
            <input type="password" placeholder="Reenter Password" name="repeatPassword" id="repeatPassword">

                @if (ViewBag.Error != null)
                {
                <label class="error" style="color: red">@ViewBag.Error</label>
                }

            <button type="submit" class="btn" onsubmit="return validateForm()" asp-action="CreateUser">Register</button>
            <button type="button" class="btn cancel" onclick="history.back()">Cancel</button>
        </form>
    </div>

    <script>
        function validateForm() {
            var username = document.getElementById("username").value.trim();
            var password = document.getElementById("password").value.trim();
            var repeatPassword = document.getElementById("repeatPassword").value.trim();
            var errorLabel = document.querySelector(".error");

            if (!username) {
                errorLabel.textContent = "Please enter a username";
                return false;
            }

            if (!password) {
                errorLabel.textContent = "Please enter a password";
                return false;
            }

            if (!repeatPassword) {
                errorLabel.textContent = "Please re-enter your password";
                return false;
            }

            if (password !== repeatPassword) {
                errorLabel.textContent = "The passwords do not match";
                return false;
            }

            errorLabel.textContent = "";
            return true;
        }
    </script>
</body>

@{
    if (TempData["msg"] != null)
    {
        <script>
            alert('@TempData["msg"]')
        </script>
    }
}
